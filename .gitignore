node_modules/
dist
dist-verify*/
.sst
.devlink/
*.tsbuildinfo
.env
.env.*
!.env.example
.webiai-docs/
.workbuddy-ai/
.agents/

# Salidas de ejecución local: no son fuente.
# `artifacts/` son capturas y reportes que escriben los propios harnesses
# (scripts/verify-*.mjs), regenerables en cada corrida.
artifacts/
vite-dev.log
# Sondas de depuración sueltas en la raíz (las suites reales viven en scripts/).
.probe-*.mjs
.verify-*.mjs
tailadmin-free-tailwind-dashboard-template-main/
packages/apps/web/modules/app/dist-orders-verify/
